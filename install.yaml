files:
  "/usr/lib/ostree/prepare-root.conf":
    content: |
      [etc]
      transient=true

    # [composefs]
    # enabled=yes

  # "/usr/lib/bootc/install/00-install.toml":
  #   content: |
  #     [install.filesystem.root]
  #     type = "btrfs"
