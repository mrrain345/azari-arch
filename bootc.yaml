packages:
  - podman
  - bubblewrap

files:
  "/usr/lib/ostree/prepare-root.conf":
    content: |
      [etc]
      transient=true
      [composefs]
      enabled=yes

  "/usr/lib/composefs/setup-root-conf.toml":
    content: |
      [etc]
      transient=true
