packages:
  - base-devel
  - mkinitcpio # required by envycontrol

postinstall:
  - su mrrain -c "git clone https://aur.archlinux.org/paru.git /tmp/paru && makepkg -si --dir /tmp/paru --noconfirm && rm -rf /tmp/paru"
  - su mrrain -c "paru -S --noconfirm oh-my-posh-bin envycontrol"
